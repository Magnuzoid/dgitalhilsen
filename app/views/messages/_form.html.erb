<%= form_for @message, user: current_user,
                       :action => :save,
                       :method => :post do |f| %>
  <%= f.text_field :msg %>
  <%= f.submit "Create", class: "message-submit" %>
<% end %>
